name: flutter_diff_search
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.14.2 <3.0.0'


dependencies:
  diff_match_patch: ^0.4.1
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^1.0.1
  test: ^1.19.1
